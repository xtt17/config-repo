server:
  port: 8091

eureka:
  client:
    enabled: true
    service-url.defaultZone: http://localhost:7000/eureka/

management:
  endpoints:  
    web:
      exposure:
        include: "*"
message: hello from git, new version






