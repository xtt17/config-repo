spring:
  cloud:
      gateway:
        server:
          webflux:
            discovery:
              locator:
                enabled: true
                lower-case-service-id: true

server:
  port: 4000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7000/eureka/